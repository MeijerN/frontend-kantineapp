import styles from './SelectElement.module.css'
import React from 'react';
import Select from "react-select";

function SelectElement({children, stylingClass}) {

    return (
        <Select/>
    );
}

export default SelectElement;